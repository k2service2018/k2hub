# บันทึกหน้าห่อ (k2hub)

หน้าห่อแต่ละตัวเคยมีคำอธิบายเขียนไว้เป็นคอมเมนต์ในไฟล์ HTML **ย้ายมาไว้ที่นี่แทน
เพราะคอมเมนต์ในไฟล์ HTML ผู้ใช้เปิด F12 แล้วเห็นหมด** ไฟล์นี้ไม่ได้ถูกลิงก์จากหน้าไหน
จึงไม่ติดไปกับหน้าเว็บ

---

## หน้าห่อพวกนี้มีไว้ทำไม

ซ่อนแถบ **"แอปพลิเคชันนี้สร้างโดยผู้ใช้ Google Apps Script"** ซึ่ง Google ใส่ไว้ใน
หน้าห่อของ Apps Script เอง และจะไม่ถูกวาดเมื่อแอปถูกฝังใน iframe ของหน้าอื่น

**ชื่อไฟล์ต้องเป็น `index.html` เท่านั้น** GitHub Pages จึงจะเสิร์ฟที่ URL หลักของโฟลเดอร์

---

## ⚠️ สามอย่างที่ห้ามใส่ — ทั้งหมดทำให้แถบบนของแอปกดไม่ได้บน iPhone

บทเรียนจาก Equipment Hub เสียเวลาไล่หาสาเหตุอยู่นาน **อย่าใส่กลับเข้าไปเพราะเห็นว่า
โจทย์หรือบทความไหนแนะนำมา**

| ห้ามใส่ | เกิดอะไรขึ้น |
|---|---|
| `sandbox` ใน `<iframe>` | เมนูในแอปกดไม่ได้ (Google Sites ใส่ให้อัตโนมัติ — นี่คือสาเหตุที่ฝังผ่าน Sites แล้วพัง) และ **Chrome บล็อกการดาวน์โหลด PDF** ที่ Sale System ต้องใช้ |
| `viewport-fit=cover` | แปลว่า "ให้เนื้อหาลอดใต้ขอบจอทุกด้าน" พอเปิดจากไอคอนหน้าจอโฮม แถบบนของแอปจะไปอยู่ใต้แถบสถานะจนมองไม่เห็น และขีด home indicator ล่างทับเนื้อหา |
| `apple-mobile-web-app-capable` | โหมดเต็มจอของ iOS ดันแถบบนของแอปไปอยู่ใต้แถบสถานะจนกดปุ่มเมนูไม่ได้ (ทดสอบแล้วทั้งเอา `viewport-fit=cover` ออกและสร้างไอคอนใหม่ ก็ยังเหมือนเดิม) |

**ที่ยอมแลก:** มีแถบที่อยู่ของ Safari บาง ๆ ค้างอยู่ แต่แอปกดได้ — ดีกว่าเต็มจอแล้วกดไม่ได้

---

## ขนาดและตำแหน่งของ iframe บน iOS — บทเรียนที่แลกมาด้วยหลายรอบ

**สรุปสั้น: อย่าเชื่อหน่วยความสูงของ CSS บน iOS Safari ให้อ่านค่าจาก `visualViewport`
แล้วตั้งเอาเอง ทั้งความสูงและตำแหน่ง**

```js
frame.style.position = 'fixed';
frame.style.top    = vv.offsetTop + 'px';   // ตัวที่ลืมแล้วเสียเวลาไปหลายรอบ
frame.style.height = vv.height    + 'px';
```

### อาการ
เปิดบน iPhone แล้ว**ส่วนบนของแอปหายไปราว 52px** — หัวข้อหน้าและปุ่มมุมขวาบน
(เช่นปุ่ม "เพิ่มลูกค้า") ถูกแถบ URL บัง **และดึงกลับลงมาไม่ได้**

### ตัวเลขจริงที่วัดได้ (iPhone, Safari)
| ค่า | ผลลัพธ์ |
|---|---|
| `document.documentElement.clientHeight` | 659 (layout viewport) |
| `visualViewport.height` | **606** (ที่มองเห็นจริง) |
| `100dvh` คายค่าออกมาเป็น | **659** ไม่ใช่ 606 |
| `visualViewport.offsetTop` | **52** เมื่อหน้าไม่มีที่ให้เลื่อน |

### กลไก
1. `height: 100%` และ `100dvh` วัดเทียบ **layout viewport (659)** ไม่ใช่ที่มองเห็น (606)
2. เฟรมจึงสูงเกินจอ หน้ามีที่ให้เลื่อน แล้ว **Safari เลื่อนเอง** เพื่อซ่อนแถบ URL
   (`overflow:hidden` กันผู้ใช้เลื่อนได้ แต่กันเบราว์เซอร์เลื่อนเองไม่ได้)
3. พอตั้งความสูงให้พอดี 606 หน้าก็ไม่มีที่ให้เลื่อน → **Safari กางแถบ URL ค้างไว้แล้ววาง
   ทับส่วนบนของหน้า** แทนที่จะเบียดพื้นที่ลงมา → `offsetTop` กลายเป็น 52
4. `position:fixed` อ้างอิง layout viewport ซึ่งเริ่มที่ y=0 = ใต้แถบ URL ขึ้นไป
   → เฟรมไปเริ่มในพื้นที่ที่มองไม่เห็น

**ตั้งแต่ความสูงอย่างเดียวไม่พอ ต้องบวก `offsetTop` ด้วย** และคำนวณใหม่ทุกครั้งที่
`visualViewport` resize หรือ scroll เพราะค่าเยื้องเปลี่ยนตอนแถบ URL หุบ/กาง

### ⚠️ เบราว์เซอร์ในแอป LINE ไม่มีอาการนี้
ค่าทุกตัวเท่ากันหมด (669) · `offsetTop` 0 — **จอเดียวกันแต่คนละเบราว์เซอร์ให้ผลคนละอย่าง**
ทดสอบผ่าน LINE อย่างเดียวแล้วสรุปว่าไม่มีปัญหาไม่ได้

### วิธีหาสาเหตุถ้าเจออีก
อย่าเดา — ใส่แถบสีทาบ**ขอบบนของ iframe** แล้วโชว์ค่าจาก `visualViewport` ลงหน้าจอ
(เปิดด้วย `#diag` เพื่อไม่ให้ผู้ใช้เห็น) มองไม่เห็นแถบสี = ขอบบนเฟรมอยู่นอกพื้นที่ที่เห็น
ตัวอย่างโค้ดดูได้จาก git history ของ `sale/index.html` (commit "Add a temporary
measuring overlay")

### ข้อที่ยังจริงจากของเดิม
- **ต้องระบุ `width`/`height` เป็นตัวเลขเสมอ** — iOS ย่อ iframe ลงเท่าเนื้อหาถ้าปล่อยให้
  ความสูงมาจาก `top`/`bottom` เฉย ๆ แล้วหน้าจะถูกตัดกลางคัน
- **ห้ามปล่อยให้หน้านอกเลื่อนได้** ไม่งั้นแถบบนของแอปจะเลื่อนหลุดจอโดยกดกลับไม่ได้
- พื้นที่ปลอดภัยของขอบจอ จัดการด้วยการ **ไม่ใส่** `viewport-fit=cover` ไม่ใช่ด้วย padding

### สคริปต์ใน sale/index.html จงใจเขียนแบบนั้น
ในไฟล์ไม่มีคอมเมนต์อธิบาย (ตามกฎว่าห้ามมีคำอธิบายให้เห็นใน F12) — เหตุผลทั้งหมดอยู่ในหัวข้อนี้
**อย่าย่อสคริปต์นั้นให้สั้นลงหรือแทนด้วยหน่วย CSS** มันคือผลของการวัดจริงข้างบน

### สถานะของแต่ละหน้า (2026-08-22)
- **sale** — แก้แล้ว ยืนยันบน iPhone จริงแล้ว
- **service** — แก้แล้ว ยืนยันบน iPhone จริงแล้ว จับได้สองสถานะ ค่า rect.top เป็น 0 ทั้งคู่
  (แถบ URL กาง: vv.height 618 · offsetTop 41 — แถบหุบ: vv.height 659 · offsetTop 0)
- **expense-claim** — เขียนใหม่ทั้งหน้าโดยลอกสคริปต์จาก `sale` มาตั้งแต่ต้น **ยังไม่ได้ยืนยันบน iPhone จริง**
  แต่ไม่ได้ใช้รูปแบบเดิมที่รู้ว่ามีปัญหา
- **tender · expense** — ยังเป็น `position:fixed; inset:0; height:100%` แบบเดิม
  **น่าจะมีอาการเดียวกัน แต่ยังไม่ได้วัด** ถ้าจะแก้ ให้วัดทีละตัวก่อนตามวิธีข้างบน

---

## ไอคอนบนแท็บ (favicon)

วาดเป็น SVG ใน data URI ไม่ต้องมีไฟล์แยกให้ 404 ได้ ถ้าไม่มีบรรทัดนี้เบราว์เซอร์จะไป
หา `/favicon.ico` ที่รากโดเมนซึ่งไม่มีอยู่ — จึงได้ลูกโลกจืด ๆ ข้างแท็บที่เขียนว่า K2

**สีต่างกันเพราะทุกแท็บเขียนว่า K2 เหมือนกันหมด สีคือสิ่งเดียวที่แยกออกตอนวางเรียงกัน**

| แอป | สี |
|---|---|
| Equipment Hub | น้ำเงิน `#4f6ef7` |
| Tender | เขียวน้ำทะเล `#0ea5a4` |
| Expense | ส้มอำพัน `#e8a13c` |
| Sale | เขียว `#2f9e44` |

---

## ไอคอนหน้าจอโฮม (`sale/icon-180.png`)

`<link rel="apple-touch-icon">` **ต้องอยู่ที่หน้าห่อเท่านั้น** ใส่ในไฟล์ของแอปไม่มีผล —
แอปรันอยู่ใน iframe ของ googleusercontent.com ส่วน Safari อ่านไอคอนจาก**หน้าบนสุด**
ซึ่งก็คือหน้าห่อ ถ้าไม่มี iOS จะเอาภาพหน้าจอของเว็บมาเป็นไอคอนแทน

ไฟล์ต้อง **180x180 พื้นหลังทึบ ไม่มีช่องอัลฟา** เพราะ iOS ปัดมุมมนเองและไม่รองรับพื้นหลังใส

---

## สคริปต์ postMessage — มีเฉพาะ Equipment Hub

**ทำไมต้องมี:** แอปนั้นเปลี่ยนหน้าด้วยการโหลด URL ใหม่ และลิงก์ทุกอันสั่ง "หน้าต่างบนสุด"
ซึ่งคือหน้าห่อ ผลคือทะลุออกไปที่ script.google.com แล้วแถบแจ้งเตือนของ Google กลับมา
หน้าห่อจึงรับหน้าที่เปลี่ยน `src` ของ iframe ให้แทน

**สองข้อความเท่านั้น**

| ข้อความ | ทำอะไร |
|---|---|
| `{k2:'hello'}` จากแอป | ตอบ `{k2:'wrapper'}` กลับ เพื่อให้แอปรู้ว่ามีหน้าห่ออยู่จริง (ไม่ใช่เดาจากจำนวนชั้น iframe ซึ่งแยก "เปิดตรง" กับ "ถูกฝัง" ไม่ออก) |
| `{k2:'nav', url}` | เปลี่ยนหน้า |

**⚠️ การตรวจผู้ส่งต้องเช็คที่โดเมน ไม่ใช่ `e.source === frame.contentWindow`** —
โค้ดของแอปไม่ได้อยู่ใน iframe ลูกโดยตรง แต่อยู่ในชั้นหลานที่ GAS สร้างซ้อนอีกที
(โดเมน `n-xxxx.googleusercontent.com`) การเทียบ `e.source` จึงไม่ผ่าน

**Tender / Sale ไม่ต้องมี** เพราะเป็นแอปหน้าเดียว กดเมนูแล้วสลับเนื้อหาในหน้าเดิม
ไม่ได้โหลด URL ใหม่

**Expense ไม่ต้องมี** เพราะไม่เคยเปลี่ยน URL ของหน้าตัวเอง — หน้าย่อย (HR Per Diem /
Admin Checklist / Dashboard) เปิดด้วย `window.open(..., '_blank')` เป็นแท็บใหม่อยู่แล้ว
**ผลที่ตามมา:** แท็บใหม่พวกนั้นเปิด script.google.com ตรง ๆ และยังเห็นแถบของ Google อยู่
ถ้าจะให้หายต้องแก้ที่ตัวแอป ไม่ใช่ที่หน้าห่อ

---

## ⚠️ `expense` กับ `expense-claim` เป็นคนละแอป

| โฟลเดอร์ | ห่อแอปอะไร |
|---|---|
| `expense` | **ระบบบันทึกค่าใช้จ่ายเดินทาง (TSAE)** ตัวเดิม คนละโปรเจกต์ Apps Script |
| `expense-claim` | **Expense Claim** ตัวใหม่ ตามแบบฟอร์ม FIN-2026-EXP-001.00 ซอร์สอยู่ที่ `expense/src/` |

ทั้งสองใช้ไอคอนสีส้มอำพันเหมือนกันเพราะเป็นสีประจำ Expense **เวลาเปิดพร้อมกันจะแยกจากแท็บไม่ออก**
ถ้าเลิกใช้ตัวเดิมเมื่อไหร่ ให้ลบโฟลเดอร์ `expense` ทิ้งแล้วเหลือตัวเดียว

> ซอร์สของแอปใหม่อยู่ใน `expense/src/` ซึ่งอยู่คนละโฟลเดอร์กับหน้าห่อของมัน —
> ตอนเอาขึ้นยังไม่รู้ว่า `expense/` เป็นของแอปเดิม ถ้าจะย้ายให้ไปอยู่ `expense-claim/src/` ก็ย้ายได้ ไม่มีอะไรอ้างพาธนี้

---

## ⚠️ URL ใน src ผูกกับ deployment ตัวที่ทีมใช้

ถ้าวันหนึ่งต้องออก **deployment ใหม่** (ไม่ใช่แค่ redeploy เวอร์ชันเดิม) ต้องมาแก้ URL
ในไฟล์หน้าห่อด้วย ไม่งั้นทีมจะยังเห็นของเก่า
